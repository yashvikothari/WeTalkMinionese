<!-- 1. emmet HTML use karishu
    2.word wrap karvanu che use alt z and type to check abcdefghijklmnopqrstuvwxyz123456789 123456789 yes it worked.
    3.ave head tag ma,title nu naam Speak Minionese karo by replacing Document in title tag.
    4. ave body tag ma,h1 ma go banana aapo
    5.live server chalu karo
    6.initial commit, publish repo aapine setup banayishakiye che
    7.understanding javascript 
    *with script tag and using external javascript file.
    *prompt as input and alert for output
    8.let say html is read from top to bottom, so instead of calling external javascript in head we will call in body last.
    9.create buton and let say it should console translate button clicked ,use id class selector in button tag for referencing butoon in javascript by using queryselector.
    10.listen to button clicked,call functionbwhich consoles translate button clicked (concept of callback applied)
    11.add textarea tag in body with id and placeholder reflecting user for entering text to get translation in banana language,try css on textarea width,height,display,margin,padding.
    12. console value when button translate clicked use console.log ("input",id.value)
    13.DOM understanding 
    15.writing query
     1. for text area tag :  document.queryselector("textarea")
     2.for class btn-primay: .btn-primary
     3.for id btninput : #btninput
     4.input element with an attribute name='translator' : "input[name='translator']"
    16.have selector blog and make cli selector quiz for fun someday
    17.do this:
    create an output div give id divoutput,later give it style border,height,width,marging,padding
    reference it in js
    use innerText to write to this div dynamically when button click happens
    18.CLI exercise for leap year and prime number.
    19.call a mock server (Postman)
    in GET enter request url






.
. -->

19 20 image.png
image.png